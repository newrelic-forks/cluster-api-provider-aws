apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
        - image: cf-registry.nr-ops.net/container-fabric/eks-bootstrap-controller-amd64:dev
          name: manager
